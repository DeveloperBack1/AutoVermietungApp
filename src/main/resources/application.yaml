spring:
  main:
    banner-mode: off
#
#    datasource:
#    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB_NAME:autovermietungapp}
#    username: ${MYSQL_USER:root}
#    password: ${MYSQL_PASSWORD:root}
#    driver-class-name: com.mysql.cj.jdbc.Driver

  datasource:
    url: jdbc:mysql://localhost:3306/autovermietungapp
    username: root
    password: Tausend1308
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect


  liquibase:
    enabled: true
    change-log: db/changelog/db.changelog-master.xml

variables:
  jwtSecret: 9ygR+2hv51AFdykOG3lE0W1pmyq1xC/4wV74L0hRRwXyqPruX8w0m/oXFPlR+RM+S3AogFdU88RAn/MeAmJhQw==
  jwtExpirationMs: 864000000